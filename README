This library aims at providing a simple messaging API for use internally in the
same JVM or for remote JVMs through an RMI interface. It does not intend to 
compete with JMS or any other high profile messaging library.
